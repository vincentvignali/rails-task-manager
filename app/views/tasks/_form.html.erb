<div class="container">
  <%= simple_form_for(@task) do |f| %>
    <%= f.input :title %>
    <%= f.input :details %>
    <%= f.input :completed if !@task.new_record? %>
    <%= f.submit class: "btn btn-primary"%>
  <% end %>
</div>
